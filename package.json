{
  "name": "dentaflow",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "pnpm --filter @dentaflow/shared build && pnpm --parallel --filter './apps/*' dev",
    "build": "pnpm --filter './packages/*' build && pnpm --filter './apps/*' build",
    "build:render": "pnpm --filter @dentaflow/shared build && pnpm --filter api build",
    "start:render": "node apps/api/dist/main.js",
    "lint": "pnpm --recursive lint",
    "test": "pnpm --recursive test",
    "db:studio": "pnpm --filter api typeorm:studio",
    "db:seed": "pnpm --filter api db:seed"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
